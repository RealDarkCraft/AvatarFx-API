# AvatarFx-API
An simple python class to generate avatarFx (Character Ai videos)

To use it, simply use the class like this : avatarFx(your_character_ai_token).generate(your_image_url, target_voice_uuid, prompt)

you can find some voice uuid on character ai website
the prompt is the text the character will say
